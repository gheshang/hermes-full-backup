§ 用户偏好：修改配置文件前先备份，再写入，再执行。高风险操作（配置变更+API调用）遵循 backup → write → execute 流程。
§ 用户要求完成每个任务后生成精简中文日志，脚本~/.hermes/log_generator.py。
§ 用户偏好：当用户要求"把文档发过来"时，发送实际文件（MEDIA:路径），而非文字摘要。
§
§ 用户偏好：技能文件不宜过长（>200行/5000字会被抱怨），需精简。精简原则：合并重叠内容、删描述性文字、表格压缩（只保留优秀/不合格两极）、模板只留骨架、详细说明移至"使用时展开"。目标压缩率≥30%。
§
§ 用户偏好：计划模式只需交付方案文档，不主动执行。用户说"执行"时需先确认是否真要执行，而非直接开工。
§
§ 用户偏好：方案文档交付时，用户说"把方案文件发过来"或"我要文档不是要你文字"时，直接发送文件附件（MEDIA:路径），不要粘贴全文内容到聊天中。