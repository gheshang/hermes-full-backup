# Cron Job: 自审-缓存清理

**Job ID:** 7468248ebd6c
**Run Time:** 2026-05-11 03:04:24
**Schedule:** 3 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行缓存和磁盘清理：
```bash
echo "=== 磁盘使用 ==="
df -h / | tail -1

echo "=== uv缓存 ==="
du -sh ~/.cache/uv/ 2>/dev/null

echo "=== uv .tmp残留清理 ==="
find ~/.cache/uv -maxdepth 1 -name ".tmp*" -type d 2>/dev/null | wc -l
du -sh ~/.cache/uv/.tmp* 2>/dev/null | tail -1
find ~/.cache/uv -maxdepth 1 -name ".tmp*" -type d -exec rm -rf {} + 2>/dev/null
echo "tmp cleaned"

echo "=== uv缓存超限检查(>2GB) ==="
bash ~/.hermes/scripts/uv-cache-guard.sh

echo "=== Hermes缓存 ==="
du -sh ~/.hermes/audio_cache/ ~/.hermes/image_cache/ ~/.hermes/cron/output/ 2>/dev/null
find ~/.hermes/audio_cache/ -mtime +7 -delete 2>/dev/null
find ~/.hermes/image_cache/ -mtime +7 -delete 2>/dev/null
find ~/.hermes/cron/output/ -mtime +7 -delete 2>/dev/null

echo "=== 过期备份清理(>15天) ==="
find ~/.hermes/backups/ -mindepth 1 -maxdepth 1 -mtime +15 -type d -exec rm -rf {} + 2>/dev/null
ls -d ~/.hermes/backups/*/ 2>/dev/null

echo "=== 最终磁盘 ==="
df -h / | tail -1
```
输出清理结果即可。

## Response

## 清理完成报告

### 磁盘状态
| 项目 | 状态 |
|------|------|
| 磁盘使用 | 22G/29G (74%)，剩余 7.8G |
| 清理前后 | 无变化（磁盘本身较健康） |

### uv 缓存
- 清理前：76MB（远低于 2GB 上限）
- `.tmp` 残留：0 个，无清理
- `uv-cache-guard.sh` 已执行，清理了不可达条目

### Hermes 缓存（已删除 >7 天的旧文件）
| 目录 | 清理后大小 |
|------|-----------|
| `audio_cache/` | 88K |
| `image_cache/` | 4.0K |
| `cron/output/` | 556K |

### 过期备份（>15 天）
- **未删除任何备份**：当前所有备份日期从 04-26 到 05-11，最新的是今天（05-11），其余均在 15 天内
- 备份保留 15 天的策略未被触发，属于正常状态

### 总结
系统磁盘 74% 使用率，uv 缓存仅 73MB，Hermes 各缓存均 <1MB，整体健康。本次清理未释放显著空间，但维持了缓存策略的正常运行。
