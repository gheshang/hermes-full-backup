# Cron Job: weekly-cache-cleanup

**Job ID:** 1ccb12cecce1
**Run Time:** 2026-06-28 03:07:10
**Schedule:** 0 3 * * 0

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行每周缓存清理：
1. 运行 uv 缓存监控脚本: bash ~/.hermes/scripts/uv-cache-guard.sh
2. pip cache purge
3. npm cache clean --force
4. 删除 uv .tmp 残留: find ~/.cache/uv -name ".tmp*" -type d -exec rm -rf {} + 2>/dev/null
5. 检查 Playwright 旧版本：当前使用的是 chromium-1217 和 chromium_headless_shell-1217，删除 ~/.cache/ms-playwright/ 下其他版本目录（保留 1217 和 ffmpeg）
6. 报告清理前后的磁盘使用率 (df -h /)

用 terminal 工具逐条执行。

## Response

## 每周缓存清理报告

### 1. uv 缓存监控
- 缓存大小：0MB / 2048MB 限制
- 清理不可达条目完成，无需额外操作

### 2. pip cache purge
- **清理 692 个文件，释放 115.5 MB**

### 3. npm cache clean --force
- 缓存已清空（仅使用 --force 提示，npm 11+ 默认行为）

### 4. uv .tmp 残留
- 未发现 `.tmp*` 残留目录

### 5. Playwright 旧版本
- 系统中不存在 `~/.cache/ms-playwright/` 目录（Playwright 未安装或缓存已被清除），无清理必要

### 6. 磁盘使用率变化
| 阶段 | 已用 | 可用 | 使用率 |
|------|------|------|--------|
| 清理前 | 16G | 14G | 53% |
| 清理后 | 16G | 14G | 53% |

释放约 **115.5 MB** 空间（pip 缓存），对整体磁盘占比影响较小。根分区剩余 14G（47% 空闲），状况健康。
