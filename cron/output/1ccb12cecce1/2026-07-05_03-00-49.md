# Cron Job: weekly-cache-cleanup

**Job ID:** 1ccb12cecce1
**Run Time:** 2026-07-05 03:00:49
**Schedule:** 0 3 * * 0

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行每周缓存清理：
1. 运行 uv 缓存监控脚本: bash ~/.hermes/scripts/uv-cache-guard.sh
2. pip cache purge
3. npm cache clean --force
4. 删除 uv .tmp 残留: find ~/.cache/uv -name ".tmp*" -type d -exec rm -rf {} + 2>/dev/null
5. 检查 Playwright 旧版本：当前使用的是 chromium-1217 和 chromium_headless_shell-1217，删除 ~/.cache/ms-playwright/ 下其他版本目录（保留 1217 和 ffmpeg）
6. 报告清理前后的磁盘使用率 (df -h /)

用 terminal 工具逐条执行。

## Response

**每周缓存清理报告 — 2026-07-05**

| 项目 | 结果 |
|---|---|
| **清理前磁盘** | 29G 总量，已用 20G (67%) |
| **uv-cache-guard** | 缓存 53MB/2048MB，已 prune 不可达条目 |
| **pip cache purge** | 无残留可清理 (0 bytes) |
| **npm cache clean** | 完成 (无具体释放量) |
| **uv .tmp 残留** | 未找到 .tmp 目录 |
| **Playwright 旧版本** | `~/.cache/ms-playwright/` 不存在，无需清理 |
| **清理后磁盘** | 已用 20G (66%)，释放约 **100MB** |

**结论**：缓存状态健康，无需干预。磁盘使用率从 67% 降至 66%，主要回收来自 uv 缓存 prune 后的元数据空间。
