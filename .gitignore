# Runtime state
*.lock
*.pid
*.db
*.db-wal
*.db-shm
state-snapshots/
backups/
checkpoints/
logs/
cache/
audio_cache/
image_cache/
processes.json
models_dev_cache.json
gateway_state.json
feishu_seen_message_ids.json
channel_directory.json
*.lock

# Python / Node
__pycache__/
*.pyc
node_modules/
venv/
.env.current.bak

# Git internals (for copied repos)
agency-agents-zh/.git
superpowers/.git
andrej-karpathy-skills/.git
tencentyun-snake-up/.git
wiki/.git

# Hermes source (reinstallable)
hermes-agent/

# Secrets (already sanitized, but double guard)
auth.json
pairing/
weixin/
